You are continuing the same app.

This is PHASE 12: COMPLETE STATE MANAGEMENT WITH RIVERPOD.

---

# 🔁 FULL CONTEXT

* Profile tab is default
* Hackathon → Team flow is STRICT
* Chat only for team members
* Skeleton loaders must show during loading
* UI must NEVER directly call repository

---

# 🎯 GOAL

Create deterministic state flow for entire app.

---

# 📁 PROVIDER FILE STRUCTURE

features/
auth/presentation/providers/auth_provider.dart
profile/presentation/providers/profile_provider.dart
hackathons/presentation/providers/hackathon_provider.dart
teams/presentation/providers/team_provider.dart
chat/presentation/providers/chat_provider.dart
community/presentation/providers/community_provider.dart
notifications/presentation/providers/notification_provider.dart

---

# 🧠 STATE DESIGN (STRICT)

Each feature MUST have:

class FeatureState {
bool isLoading;
String? error;
DataType? data;
}

---

# 🔐 AUTH PROVIDER

class AuthNotifier extends StateNotifier<AuthState>

On init:

* check Supabase session
* set:
  authenticated OR unauthenticated

Methods:

* login()
* register()
* logout()

---

# 👤 PROFILE PROVIDER

class ProfileNotifier

On login:
→ fetch profile

Methods:

* fetchProfile()
* updateProfile()

Cache profile locally

---

# 🏆 HACKATHON PROVIDER

class HackathonNotifier

Methods:

* fetchHackathons()
* loadMore()

Use pagination

---

# 👥 TEAM PROVIDER

class TeamNotifier

Methods:

* fetchRecommendedTeams(hackathonId)
* createTeam()
* joinTeam()

IMPORTANT:

* must call matching logic before returning teams

---

# 💬 CHAT PROVIDER

Use StreamProvider:

final chatProvider = StreamProvider.family<List<Message>, String>((ref, teamId) {
return chatRepository.subscribeMessages(teamId);
});

---

# 🌐 COMMUNITY PROVIDER

* fetchPosts()
* createPost()
* vote()

---

# 🔔 NOTIFICATION PROVIDER

* fetchNotifications()
* markAsRead()

---

# 🔄 UI INTEGRATION RULE

Use:
ref.watch(provider)

Handle:

* loading → show skeleton
* error → show retry
* data → show UI

---

# 🚫 FORBIDDEN

* No direct DB calls in UI
* No global mutable state

---

# OUTPUT

* All providers implemented
* Full state lifecycle
* Connected to repositories

END OF PHASE 12
